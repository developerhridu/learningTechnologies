function message(name, age) {
  // const name = "Jesse";
  // const age = 40;
  return name + " is " + age + " years old.";
};

function sumOfTwo(x, y){
  return x + y;
}

export { message, sumOfTwo };

